version: '3.11'

services:
  mcp_server:
    build: .                     # 使用当前目录的 Dockerfile 构建
    ports:
      - "8000:8000"              # 主机:容器 端口映射
    volumes:
      - .:/app                  # 可选：挂载代码目录（开发时热更新用）
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped